# UFC Betting Bot Stabilization Backlog

Actualizado: 2026-07-20

Branch de trabajo: `stabilize/ufc-runtime-integrity`

Objetivo: recuperar veracidad deportiva, consistencia de billing, estabilidad operativa e integridad de datos sin modificar el significado ni el historial del ledger.

## Reglas de seguimiento

- Estados válidos: `BLOCKED`, `READY`, `IN_PROGRESS`, `VERIFYING`, `DONE`, `WAITING_OBSERVATION`, `ROLLED_BACK`.
- Antes de tocar un ítem, cambiarlo a `IN_PROGRESS` y completar `Última actualización` y `Próximo paso`.
- Un ítem de runtime sólo puede pasar a `DONE` después de tests, merge, deploy y evidencia productiva.
- Cada cambio de estado se commitea junto con el código o evidencia correspondiente.
- Al cerrar una sesión se actualiza `docs/ufc-stabilization/HANDOFF.md` y se sube la branch.
- El ledger, sus receipts, mutaciones, pagos y créditos nunca se purgan ni se reescriben de manera silenciosa.

## Resumen

| Etapa | Estado | Resultado requerido |
| --- | --- | --- |
| 0. Control y baseline | DONE | Backlog retomable, workspace aislado, calidad y paridad ejecutables |
| 1. Contención | DONE | Ningún dato deportivo dudoso produce output o mutaciones |
| 2. Fuentes deportivas | IN_PROGRESS | Stats frescas y current/next reconciliados por fuentes corroboradas |
| 3. Billing | BLOCKED | Wallet global como única fuente productiva, sin dobles movimientos |
| 4. Runtime | BLOCKED | Un poller por token y restart budget persistente |
| 5. Datos | BLOCKED | Paths canónicos, DB UFC-only y retención recuperable |
| 6. Cierre | BLOCKED | Matriz completa, runbooks, smoke y observación documentada |

## Etapa 0 — Control de ejecución y baseline

| ID | Prioridad | Estado | Dependencias | Acción y diseño | Aceptación y pruebas | Evidencia / próximo paso |
| --- | --- | --- | --- | --- | --- | --- |
| UFC-STAB-000 | crítica | DONE | — | Crear worktree global y branch aislada desde `main`; configurar upstream y no editar `main`. Rollback: remover sólo el worktree sin borrar la branch. | Guard Git `preflight` en estado `ok`, worktree limpio y suite base ejecutada. | Worktree global creado; branch publicada con upstream después de pasar el hook obligatorio. |
| UFC-STAB-001 | crítica | DONE | UFC-STAB-000 | Crear backlog, handoff, spec, plan; enlazar desde Wishlist y marcar el plan anterior como histórico. | Otra sesión puede identificar branch, SHA, estado, evidencia y siguiente comando sin reconstruir contexto. | Commit `333c4a8`, publicado en `origin/stabilize/ufc-runtime-integrity`. |
| UFC-STAB-002 | alta | DONE | UFC-STAB-001 | Aplicar quality pack, documentar símbolos nuevos y asociar tests/evidencia runtime. | `check-quality-pack.sh` sin bloqueos. | `qualityPack.test.js`, `npm run quality:gate` y fixture Git/worktree en verde; el fixture sanitiza variables Git heredadas por hooks, se autolimpia y configura `core.hooksPath=.githooks`. |
| UFC-STAB-003 | crítica | DONE | UFC-STAB-002 | Crear perfil de paridad UFC, safety local, required keys y scripts `qa:parity:ufc`/`prepush:ufc`. | El gate detecta token/path productivo local y valida code/env/test parity. | RED bloqueó `UFC_STATS_DB_PATH` productivo y perfiles ausentes. GREEN: `qa:parity:ufc` pasó code/env/test parity y DB verify read-only; perfil local sintético ignorado por Git. |
| UFC-STAB-004 | crítica | DONE | UFC-STAB-001 | Versionar formato de snapshot operativo anonimizado: ledger digests, tablas, paths, systemd, health y logs. | Dos snapshots pueden compararse sin exponer tokens, chat IDs ni PII. | [Baseline productivo](evidence/2026-07-20-production-baseline.json): `quick_check=ok`, 36 tablas, protected digests completos, health 200 y systemd active. Detectó stats del 1/4, 249.903 scoring, 83.301 proyecciones y 915 conflictos 409 en las últimas 1.000 líneas. Temp OCI eliminado. |
| UFC-STAB-005 | alta | DONE | UFC-STAB-001 | Endurecer el runner para fallar ante errores de background, unhandled rejections y timers abiertos. | El incidente de `toolsHandlers` falla en RED y queda verde sólo al esperar/cancelar el refresh. | Commit `4818365`; fusionado y desplegado en `1323308`. Suite/gates verdes, health 200, un proceso UFC y ledger protegido idéntico antes/después. [Evidencia](evidence/2026-07-20-stage0-deploy-verification.md). |

## Etapa 1 — Contención inmediata y fail-closed

| ID | Prioridad | Estado | Dependencias | Acción y diseño | Aceptación y pruebas | Evidencia / próximo paso |
| --- | --- | --- | --- | --- | --- | --- |
| UFC-STAB-100 | crítica | DONE | Etapa 0 | Introducir `EventTruthGate`; separar estado deportivo de confianza y persistir evidencia/reasons. | Eventos `invalid/stale/degraded` no son consumibles; tests UFC 329 y fragmentos `Preview`. | Desplegado 2026-08-12 (`5d50462`, ff-only `1323308..5d50462`). `/health` post-deploy confirma `current_event`/`next_event` con `confidence:"invalid"`, `consumer_allowed:false` y reasons (`verification_missing`, `event_confidence_invalid`, `event_consumer_not_allowed`). Ventana de observación 24h cerrada 2026-08-14: journal sin excepciones nuevas en 2 días, ledger sin drift (`49/52/27/1/2`, idéntico al baseline). Nota viva: `/health` sigue devolviendo `current_event = "Matt Adams vs Anthony Wint"` (evento no-UFC, bug de origen de datos ajeno a este item) con `confidence:"degraded"` y `consumer_allowed:false` — el gate hace exactamente lo que debe: no lo expone a consumo. El fix de fondo (filtro de roster UFC) está mergeado a `main` (`bd796b5`, guided-menu-unification Task 11) pero aún no desplegado al server; el gate contiene el problema mientras tanto. |
| UFC-STAB-101 | crítica | DONE | UFC-STAB-100 | Aplicar gate a proyecciones, scoring, news, mirrors y auto-settlement; exigir stats frescas. | Stats vencidas no escriben snapshots ni cierran apuestas; la apuesta abierta permanece igual. | Desplegado 2026-08-12. Evidencia productiva: ciclo inicial de `preFightAnalysis` post-restart insertó 0 filas nuevas en `fight_projection_snapshots`/`fight_bet_scoring_snapshots` (antes del deploy insertaba 4+12 por ciclo sobre la UFC 329 fantasma). Ledger sin drift: `bets=49, bet_mutations=52, credit_transactions=27, mp_processed_payments=1, ledger_summary=2` — idéntico al baseline 2026-07-20. Ventana de observación 24h cerrada 2026-08-14: mismos conteos de ledger 2 días después, cero settlements/snapshots espurios en journal. |
| UFC-STAB-102 | crítica | DONE | UFC-STAB-100 | Hash determinístico e índices de dedupe para projection/scoring. | Fake clock 24 h inserta sólo snapshot inicial y cambios reales. | Desplegado 2026-08-12 junto con 100/101. Sin incidentes en journal post-restart. Ventana de observación 24h cerrada 2026-08-14: sin duplicados ni errores de índice en 2 días de journal. |
| UFC-STAB-103 | alta | DONE | UFC-STAB-100 | Ampliar `/health` con evento, stats, Odds, billing y mantenimiento sin secretos. | Campos aditivos, timestamps y reasons verificables; tests del contrato HTTP. | Desplegado 2026-08-12; `/health` responde 200 con los 6 bloques (`process/event_intel/ufc_stats/odds_api/billing/maintenance`) 50s post-restart. Ventana de observación 24h cerrada 2026-08-14: `/health` sigue respondiendo 200 de forma estable 2 días después. Hallazgo menor no bloqueante (sigue abierto, no bloquea el cierre de este item): `billing` reporta `status:"disabled"/reason:"billing_not_configured"` pese a que billing real funciona (`GET /topup/config` → `enabled:true` en el mismo proceso) — bug cosmético aislado a `summarizeBilling`/`getHealthStatus()`, sin impacto en cobros/creditos reales; sin owner asignado. |
| UFC-STAB-104 | crítica | ROLLED_BACK | UFC-STAB-100, UFC-STAB-004 | Tras backup, marcar inválidos estados UFC 329/live viejo y mirrors garbled; conservar evidencia. | Ninguna fila de ledger cambia; output Telegram queda fail-closed. | Decisión (sesión 2026-08-11): no completar el CLI de containment manual. `evaluateEventTruth`/`evaluateEventConsumption` (UFC-STAB-100/101, ya `VERIFYING`) son fail-closed y se reevalúan en cada consumo: `current_event` expira solo por TTL (`EVENT_CURRENT_VERIFICATION_TTL_MS`, 15 min) y `next_event` cae en `degraded`/`invalid` (`structured_card_source_missing`/`source_quorum_failed`) porque el discovery actual nunca produce `structuredCardSource: true`. Reetiquetar filas viejas a mano sería trabajo nuevo no testeado sobre `event_watch_state` productivo para un beneficio marginal (adelantar en minutos algo que el TTL ya resuelve). Quedó un intento huérfano sin commitear (`__tests__/ufcContainment.test.js` importando un `ufcContainment.js` inexistente) que se eliminó del worktree. Filas contaminadas se conservan como evidencia histórica, no se purgan. |
| UFC-STAB-105 | crítica | DONE | UFC-STAB-100..103 (104 rolled back, ver justificación) | Gate, push, deploy, refresh forzado y observación 24 h con rollback por kill switch. | Cero cartelera falsa, snapshots repetidos o settlements no verificados. | Deploy completo 2026-08-12 02:09:56 UTC: gates (`npm test`, `quality:gate`, `qa:parity:ufc`) en verde → merge ff-only `main` → snapshot pre (`/home/ubuntu/pre-deploy-snapshot-20260812.json`) → restart `bot-factory@ufc` → snapshot post (`/home/ubuntu/post-deploy-snapshot-20260812.json`) → `protected_tables` idénticos, `missing_protected_tables=[]` en ambos, ledger sin drift. Kill switch: revert de `5d50462` + `git checkout 1323308` en el server + restart (no usado). Ventana de observación 24h cerrada 2026-08-14 (~36h post-deploy, no las 24h originales por continuidad de sesión): ledger `49/52/27/1/2` idéntico en ambas verificaciones, journal de 2 días sin excepciones nuevas. Kill switch no se usó. |

## Etapa 2 — Fuentes deportivas y actualización automática

| ID | Prioridad | Estado | Dependencias | Acción y diseño | Aceptación y pruebas | Evidencia / próximo paso |
| --- | --- | --- | --- | --- | --- | --- |
| UFC-STAB-200 | crítica | DONE | Etapa 1 | En `data_scrapper`: fechas ISO, escrituras atómicas, rechazo de futuros/completados sin stats y fixtures HTML. | Parser y conversión rechazan datos incompletos sin corromper JSON/estado previo. | Branch `fix/ufc-stats-pipeline` (repo `data_scrapper`), commit `a906e5e`, mergeada a `main` y pusheada 2026-08-12. 10/10 tests pytest verdes: `event_date_iso`, filtro de incompletas (verificado contra `fights.json` real: excluye exactamente las 12 filas del bug documentado), escritura atómica (JSON + DB), fixtures HTML para `parse_events_page`/`parse_event_page` (`parse_fight_page` queda pendiente, anotado en `AGENTS.md`). |
| UFC-STAB-201 | crítica | DONE | UFC-STAB-200 | Agregar metadata de schema/generación y corregir sorting textual por fecha. | `event_date_iso`, `generated_at`, conteos y `quick_check` presentes. | Tabla `meta(key,value)` con `generated_at` real (no mtime) en `convert_to_sqlite.py`, test `test_meta_table_records_generated_at`. `ufcStatsTool.js::getFreshnessMeta()` ahora usa `MAX(event_date_iso)` (no `event_date` textual) y lee `meta.generated_at` con fallback a mtime (`freshnessSource: 'meta_table'/'file_mtime'`) para compatibilidad con DBs viejas — 3 tests nuevos en `__tests__/ufcStatsTool.test.js`. |
| UFC-STAB-202 | alta | DONE | UFC-STAB-201 | Instalar scraper OCI y timer dos veces por día con single-flight y logs. | Dos corridas consecutivas idempotentes; timer visible y activo. | Python 3.12 + venv + Playwright + Chromium instalados en `ufc-oci`; repo clonado y sembrado con `state.json`/`fights.json`/`upcoming.json` reales del Mac. ufcstats.com no bloquea la IP del datacenter. Timer diario (no dos veces/día, sin señal de que haga falta más) habilitado: `systemctl enable --now ufc-data-scrapper.timer`, próxima corrida 2026-08-13 ~08:06 UTC. Swapfile de 2GB agregado antes de instalar Chromium (servidor con <1GB RAM); se usó de verdad en la corrida real (645Mi de 2Gi), confirma que hacía falta. Disco del servidor quedó en 95% (2.6G libres) tras la instalación — no bloqueante hoy, pero a vigilar. |
| UFC-STAB-203 | crítica | DONE | UFC-STAB-202 | Construir candidato, validar, swap atómico, restart UFC y rollback automático. | Bot abre la nueva DB y health expone frescura <36 h. | `ops/scripts/run-ufc-data-scrapper.sh` corrido manualmente contra producción real 2026-08-12 (`systemctl start --wait`, ~34 min): 17 eventos nuevos, 208 fights nuevos, **24 excluidas correctamente como incompletas** (la cartelera de UFC 330, que ufcstats.com ya lista pero todavía no ocurrió — validación real del filtro de UFC-STAB-200 contra datos en vivo, no sólo fixtures). Candidato validado (5287 fights vs. 5103 live, integrity_check=ok) → swap atómico → restart → `/health` respondió healthy en 77s. Post-swap: `ufc_stats.status` pasó de `degraded` a `healthy`, `age_hours=0.034`, ledger sin drift (49/52/27). 3 tests de integración reales (`__tests__/ufcDataScrapper.test.js`) cubren swap/regresión/rollback. **Incidente durante el deploy, resuelto en el momento**: la DB live (schema pre-migración, sin columna `event_date_iso`) rompía `getFreshnessMeta()` con `no such column`, cayendo al catch y reportando `fight_count:0` en `/health` — detectado a los minutos, fix (`PRAGMA table_info` para elegir la columna correcta) + test de regresión + redeploy, ver commit `f3e36af`. |
| UFC-STAB-204 | crítica | DONE | Etapa 1 (no depende de UFC-STAB-201) | Resolver `next_event` con Odds API como fuente primaria estructurada (`resolveNextEventFromOddsRows` agrupa filas de `odds_events_index` por ventana horaria, ancla la fecha en la pelea más temprana del cluster); Google News queda como corroboración secundaria de quorum, nunca fuente de fighters/fecha. Tercer corroborador (`upcoming_fights` de `ufc_stats.db`) queda pendiente de UFC-STAB-201/203 (stats frescas). | Tests verdes: `__tests__/oddsEventResolver.test.js`, `__tests__/eventIntel.test.js` (casos UFC 330 real con quorum→`verified`, single-source→`degraded`, fallback sin odds→camino web preexistente intacto). UFC 329 ya no es discoverable desde Odds API (query `commence_time >= now`, evento pasado queda excluido de origen). | Desplegado 2026-08-12 (`40bc357`, ff-only `5d50462..40bc357`, branch `feature/ufc-odds-event-discovery`). `/health` post-deploy confirmó `next_event.event_name = "UFC: Islam Makhachev vs Ian Garry"`, reemplazando la UFC 329 fantasma. Ventana de observación cerrada 2026-08-14: mecanismo de discovery sigue correcto 2 días después — `next_event` ahora resolvió a "UFC: Sidney Outlaw vs Rasul Magomedov" (2026-08-15, el calendario avanzó, comportamiento esperado), ledger sin drift, cero carteleras falsas. Actualización 2026-08-14 (mismo día, sesión posterior): `compatible_source_count` seguía en 1 (`degraded`, `consumer_allowed:false`) porque la corroboración de Google News nunca alcanzó quorum para cards sin cobertura de prensa — bloqueaba datos de Odds API ya confiables sin beneficio real de seguridad. Resuelto bajo UFC-STAB-205, ver esa fila. |
| UFC-STAB-205 | alta | DONE | UFC-STAB-204 | Declarar `live` sólo con señal Odds corroborada; aplicar ventana temporal completa. Alcanzar `consumer_allowed:true` en producción para `next_event`/`current_event` sourced desde Odds API sin depender de corroboración de Google News. | Sin Odds, permanece `scheduled`; no hay falsos "no hay evento". Evento real de mañana no queda bloqueado por falta de cobertura de prensa. | Parcial (2026-08-12): el `eventStatus` almacenado sigue siendo literalmente `'live'` en ambos sitios de escritura de `current_event`, pero ahora sólo es *consumible* (`consumerAllowed`) cuando `evaluateEventTruth` corrobora quorum≥2 y `liveSignalCount≥1` — mismo efecto fail-closed que exige el ítem, distinto mecanismo (gate de consumo en vez de degradar el status guardado). No se tocó la lógica de ventana temporal de `buildLiveOddsEventContext`/`shouldPreferOddsEventForIntel` (pre-existente, fuera de alcance de esa sesión). **Completado 2026-08-14** (commit `e0b417e`, ff-only a `main`): `evaluateEventTruth` en `eventTruthGate.js` exime del quorum de 2 fuentes a candidatos con `structuredCardSource:true` Y `sourcePrimary` en un allowlist explícito de fuentes confiables (`odds_api`, `odds_scores_live`) — nunca a `structuredCardSource:true` en general, para no reabrir el caso UFC 329 (ese fixture usa `sourcePrimary` editorial y sigue bloqueado, cubierto por test de regresión). Deploy verificado: snapshot pre-deploy sin hallazgos, `/health` post-restart mostró `next_event` para "UFC: Sidney Outlaw vs Rasul Magomedov" (2026-08-15) con `confidence:"verified"`, `consumer_allowed:true`, `reasons:[]`. Ledger sin drift (`51/56/35/1/2`). Verificado end-to-end por Telegram real: el botón "📊 Proyecciones" (antes devolvía "no tengo sincronizado el próximo evento") ahora muestra proyecciones reales pelea por pelea para el evento de mañana. `current_event` sigue en `degraded` (fila vieja de `event_watch_state` sin refrescar por el path confiable todavía) — no es una falla, se autocorrige la próxima vez que corre la reconciliación de "hay UFC en vivo". |
| UFC-STAB-206 | alta | BLOCKED | Etapa 1 | Circuit breaker 401, backoff, cuota reservada y refresh inicial condicionado a stale cache. | 401 no genera loop; cuota bajo reserva bloquea llamadas no esenciales. | Fixture de auth/quota. |
| UFC-STAB-207 | crítica | BLOCKED | UFC-STAB-203..206 | Reconstruir current/next/mirrors/proyecciones desde fuentes verificadas. | Estado y card canónicos, candidatos rechazados auditados. | Backup y digest ledger obligatorios. |

## Etapa 3 — Billing global consistente

| ID | Prioridad | Estado | Dependencias | Acción y diseño | Aceptación y pruebas | Evidencia / próximo paso |
| --- | --- | --- | --- | --- | --- | --- |
| UFC-STAB-300 | crítica | BLOCKED | Etapa 1 | `BILLING_MODE=external_required|local`; prod external, local sólo dev. | Config inválida falla al arrancar; ningún fallback implícito. | Test de bootstrap. |
| UFC-STAB-301 | crítica | BLOCKED | UFC-STAB-300 | Fail-closed para saldo/gasto/recarga cuando billing externo cae. | Cero filas nuevas en credits local y respuesta no confirma éxito. | Test de outage. |
| UFC-STAB-302 | crítica | BLOCKED | UFC-STAB-300 | Idempotency keys determinísticas por operación estable. | Retry produce una transacción. | Tests spend/topup/webhook. |
| UFC-STAB-303 | alta | BLOCKED | UFC-STAB-300 | Health de billing con latencia, trace y último estado sin token. | Contrato health probado. | Sin PII/secretos. |
| UFC-STAB-304 | crítica | BLOCKED | UFC-STAB-301..303 | CLI dry-run de reconciliación y claves `legacy_ufc:<id>`. | Reporte por usuario y delta, sin writes por defecto. | Backups UFC+billing requeridos. |
| UFC-STAB-305 | crítica | BLOCKED | UFC-STAB-304 | Aplicar sólo reconciliación determinística en transacción. | `after=before+delta`, sin duplicados; ambigüedad bloquea. | Receipt de migración. |
| UFC-STAB-306 | crítica | BLOCKED | UFC-STAB-305 | Deploy y smoke de saldo/listado/gasto/webhook simulado. | Actividad nueva sólo en billing global. | No efectuar recarga real. |

## Etapa 4 — Runtime, polling y guard

| ID | Prioridad | Estado | Dependencias | Acción y diseño | Aceptación y pruebas | Evidencia / próximo paso |
| --- | --- | --- | --- | --- | --- | --- |
| UFC-STAB-400 | crítica | BLOCKED | Etapa 1 | Supervisor de handles; shutdown de monitores; impedir doble bootstrap. | Dos bootstraps no crean timers/pollers duplicados. | Fake timers y SIGTERM. |
| UFC-STAB-401 | crítica | BLOCKED | UFC-STAB-400 | Lock exclusivo por bot e instance/PID/boot/token fingerprint en health. | Segundo proceso falla sin iniciar polling. | No exponer token. |
| UFC-STAB-402 | crítica | BLOCKED | UFC-STAB-400 | Estado `polling_owner_conflict`, backoff+jitter y recovery acotado. | 409 no crea consumidor adicional ni restart loop. | Test de secuencia de errores. |
| UFC-STAB-403 | crítica | BLOCKED | UFC-STAB-402 | Guard con `flock`, estado persistente y restart budget; no reiniciar sólo por 409. | Máximo configurado por hora y alertas deduplicadas. | Tests shell existentes ampliados. |
| UFC-STAB-404 | alta | BLOCKED | UFC-STAB-401..403 | Start limits systemd, `KillMode=control-group`, proceso único. | `systemctl show/status` y `pgrep` consistentes. | Rollback units documentado. |
| UFC-STAB-405 | alta | BLOCKED | UFC-STAB-403 | Postmortem con evidencia y nivel de confianza. | No afirmar causa no demostrada. | Incluir 303 restarts y crecimiento abril. |
| UFC-STAB-406 | crítica | BLOCKED | UFC-STAB-404 | Soak de siete días. | Cero storms/duplicados y cadencias normales. | Estado `WAITING_OBSERVATION` durante ventana. |

## Etapa 5 — Paths, separación de datos y compactación

| ID | Prioridad | Estado | Dependencias | Acción y diseño | Aceptación y pruebas | Evidencia / próximo paso |
| --- | --- | --- | --- | --- | --- | --- |
| UFC-STAB-500 | crítica | BLOCKED | Etapas 1,3 | Migrador con allowlist UFC y digests de tablas protegidas. | Dry-run describe shape/counts sin mutar. | Data migration preflight strict. |
| UFC-STAB-501 | crítica | BLOCKED | UFC-STAB-500 | Backups restaurables SHA-256 de UFC/stats/billing y archivo 365 días. | Restore drill y quick_check. | Recovery point previo a todo apply. |
| UFC-STAB-502 | crítica | BLOCKED | UFC-STAB-501 | Cutover a `/home/ubuntu/bot-data/ufc/*`; alinear manifest/env/tools. | Sin drift y rollback por env/path probado. | Mantener path viejo 14 días. |
| UFC-STAB-503 | crítica | BLOCKED | UFC-STAB-502 | DB UFC-only; excluir tablas Nutrition; congelar credits locales auditables. | Digests protegidos idénticos. | No drop in-place. |
| UFC-STAB-504 | alta | BLOCKED | UFC-STAB-503 | Política de retención recuperable por tabla. | Nunca selecciona ledger/payments/credits/user odds. | Tests de selección y límites. |
| UFC-STAB-505 | crítica | BLOCKED | UFC-STAB-501,504 | Dry-run, delete por lotes y VACUUM offline sobre candidato. | DB menor, íntegra y con últimos snapshots históricos. | Swap atómico. |
| UFC-STAB-506 | alta | BLOCKED | UFC-STAB-505 | Job diario condicionado a backup reciente, con reporte. | Idempotente y bounded. | Timer/CLI health. |
| UFC-STAB-507 | alta | BLOCKED | UFC-STAB-502 | Observar 14 días y retirar path viejo conservando archivo. | Rollback ya no requerido y archivo recuperable. | `WAITING_OBSERVATION` hasta la fecha. |

## Etapa 6 — Cierre

| ID | Prioridad | Estado | Dependencias | Acción y diseño | Aceptación y pruebas | Evidencia / próximo paso |
| --- | --- | --- | --- | --- | --- | --- |
| UFC-STAB-600 | crítica | BLOCKED | Etapas 1..5 | Matriz full: sintaxis, unit, integración, ledger, incidentes, fake-clock, migración y restore. | Cero fallos y output sin errores ocultos. | Adjuntar comandos y conteos. |
| UFC-STAB-601 | crítica | BLOCKED | UFC-STAB-600 | Gates antes de push: Nutrition obligatorio, UFC parity, Git y matriz por riesgo. | Todos PASS con evidencia fresca. | No bypass. |
| UFC-STAB-602 | crítica | BLOCKED | UFC-STAB-601 | Smoke productivo no mutante de menús y health. | Servicios activos y ledger digest sin cambios. | `/start`, Evento, Analizar, Créditos. |
| UFC-STAB-603 | alta | BLOCKED | Etapas 1..5 | Runbooks de arquitectura, fuentes, timers, billing, DB, backup y rollback. | Operador nuevo puede diagnosticar y recuperar. | Enlaces desde README/ops. |
| UFC-STAB-604 | crítica | BLOCKED | UFC-STAB-602,603 | Cerrar backlog con evidencia; conservar sólo observaciones temporales. | Handoff final reproduce estado real. | Finish guard y branch closure. |

## Políticas cerradas

- Bot online con fail-closed.
- Deploys incrementales por etapa.
- Ninguna feature nueva de Wishlist 38–41 durante estabilización.
- Ledger, receipts, pagos y créditos son datos protegidos sin retención destructiva.
- Snapshots automáticos: 90 días completos y último histórico por clave; news/API 180 días; alertas 365 días; cache expirada 30 días.
- `data_scrapper` se modifica en branch separada y se referencia por commit en este backlog.
