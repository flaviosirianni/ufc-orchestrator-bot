# UFC Stabilization Handoff

Actualizado: 2026-08-14

Estado general: Etapa 0 cerrada. Etapa 1 (UFC-STAB-100/101/102/103/105) **DONE** — ventana de observación 24h cerrada 2026-08-14, sin drift ni incidentes; UFC-STAB-104 rolled back. Etapa 2 sustancialmente completa: UFC-STAB-200/201/202/203 DONE (data_scrapper corriendo solo, diario, en `ufc-oci`); UFC-STAB-204 **DONE** (mecanismo correcto, ver nota de `consumer_allowed` abajo); UFC-STAB-205 parcial (`IN_PROGRESS`); UFC-STAB-206/207 sin empezar. Plan A/B/C completo y observado.

**Trabajo nuevo esta sesión (2026-08-12/14), fuera del plan A/B/C, mismo repo**: implementación completa de `docs/superpowers/plans/2026-08-12-guided-menu-unification.md` (11 tareas) — reemplaza el ruteo por keywords en modo guiado por ruteo puramente por estado de sesión/botón (elimina la clase de bug que causaba que el bot "dijera que sí" a registrar una apuesta y no lo hiciera), engancha identidad de pelea (`fight_id` estable de `event_fight_mirror`) a los botones de "Registrar apuesta"/"Analizar quotes" desde Evento y desde Análisis, parte las proyecciones de evento en un mensaje por pelea, y de paso arregla el bug de `current_event` mostrando peleas que no son UFC (el mismo "Matt Adams vs Anthony Wint" mencionado en esta sesión de estabilización). Mergeado a `main` (`bd796b5`) y pusheado, con dos rondas de revisión (spec compliance + calidad de código) por cada tarea. **Todavía no desplegado a `ufc-oci`** — ver "Próximo paso exacto".

Branches: `stabilize/ufc-runtime-integrity` (Fase A) + `feature/ufc-odds-event-discovery` (Fase B) + `feature/ufc-data-scrapper-timer` + `fix/ufc-stats-legacy-schema-compat` (Fase C) en `ufc-orchestrator-bot`, todas mergeadas a `main` por fast-forward. `fix/ufc-stats-pipeline` en el repo `data_scrapper`, también mergeada. Sesión 2026-08-12/14: `feature/guided-menu-unification` (11 tareas + fixes de revisión, `bd796b5`) y `docs/close-ufc-stab-observation-window` (`37887c7`), ambas mergeadas a `main` por fast-forward y borradas tras el merge.

Producción modificada durante esta ejecución: sí, hasta Fase C. `main`/OCI en `ufc-orchestrator-bot` desplegado en `f3e36af458bf726b52f9f4fe40dd4402d8b201e0`. `ufc_stats.db` regenerado por primera vez de forma automatizada 2026-08-12 14:36 UTC. **`main` local avanzó 30 commits más allá de lo desplegado** (guided-menu-unification completo + el cierre de esta ventana de observación) — el server sigue corriendo `f3e36af`, no lo que está en `main` ahora.

## Último checkpoint

- Worktree global creado desde `main` en commit `1afddfa`.
- Git preflight del worktree: `ok`.
- Backlog/spec/plan publicados en `origin/stabilize/ufc-runtime-integrity` mediante commit `333c4a8`; el pre-push Nutrition completo pasó.
- `npm install` completado desde `package-lock.json`.
- Baseline inicial: `npm test` terminaba con exit code 0 aunque `toolsHandlers` disparaba `Background cache refresh failed: Should not read Google Sheet when sqlite cache exists`.
- UFC-STAB-002 cerrado: quality pack versionado, instalador compatible con worktrees, `npm run quality:gate` y regresión `qualityPack.test.js` en verde.
- UFC-STAB-005 cerrado: el cache SQLite ya no dispara refresh sin ownership y el runner falla ante rechazos no manejados o `Timeout` residuales; `npm test` queda verde sin `Background cache refresh failed`.
- UFC-STAB-003 cerrado: perfiles UFC versionados, paths locales protegidos, token local/live comparado únicamente por hash y comandos `qa:parity:ufc`/`prepush:ufc` disponibles. El gate real pasó paridad de código, entorno, suite integral y verificación DB read-only.
- UFC-STAB-004 cerrado: CLI `ufc:baseline:capture`, esquema v1, cuatro fixtures anonimizados y pruebas de cero escrituras/PII verdes. La evidencia productiva sanitizada quedó en `docs/ufc-stabilization/evidence/2026-07-20-production-baseline.json`; el temp OCI exacto fue eliminado tras validar copia semántica.
- Baseline productivo confirmado: servicio `active/running`, health 200, DB UFC `quick_check=ok`, 36 tablas y seis digests protegidos completos. Señales críticas: `ufc_stats.db` mtime `2026-04-01`, 249.903 scoring snapshots, 83.301 projection snapshots, tablas Nutrition dentro de UFC y 915 conflictos Telegram 409 en las últimas 1.000 líneas. El health del proceso actual muestra cero conflictos porque su contador reinició con el proceso; journal conserva la evidencia histórica.
- Incidente de test cerrado: `qualityPack.test.js` heredó variables Git del primer `pre-push` y creó el commit temporal `4904932`. Se restauraron branch/índice sin tocar el working tree, se eliminaron sólo sus artefactos y se agregó una regresión que sanitiza las variables locales de Git. La branch remota fue corregida bajo lease exacto y local/origin/remoto coinciden en `4818365`.
- Preflight de merge detectó y corrigió la última secuela del mismo incidente: `core.bare` común había quedado en `true`. Se restauró a `false`, ambos worktrees vuelven a resolverse correctamente y la regresión ahora exige que HEAD y el modo no-bare permanezcan invariantes.
- Etapa 0 fusionada a `main`, publicada y desplegada por fast-forward `1afddfa..1323308`. `billing-service` y `bot-factory@ufc` quedaron `active/running`; UFC tiene un único proceso de su cgroup, health 200 y `NRestarts=0`.
- La captura post-deploy read-only confirmó `quick_check=ok` y coincidencia exacta de los seis conteos/digests protegidos. El archivo temporal remoto fue eliminado. Evidencia: `docs/ufc-stabilization/evidence/2026-07-20-stage0-deploy-verification.md`.
- El arranque UFC tardó aproximadamente 218 segundos hasta abrir `/health`; queda como señal de performance/runtime a investigar. Desde el boot verificado hubo cero 409, pero se volvió a publicar UFC 329 y se escribieron 5 proyecciones/15 scoring desde stats vencidas. La Etapa 1 debe contener esto antes de cualquier reconstrucción.
- UFC-STAB-100 local completo: `EventTruthGate` puro, migración aditiva, lectura legacy `invalid/verification_missing`, decisiones append-only y discovery web auditado como no consumible. Tests de UFC 329, `Preview`, stale-live, store legacy y suite completa verdes. No se desplegó todavía este cambio.
- UFC-STAB-101 local completo: revalidación al consumir, freshness stats por mtime con máximo 36 h, bloqueos de news/proyección/scoring/mirrors/settlement y vista segura para Telegram. Tests positivos y negativos verdes; una DB real conservó apuesta pending y conteo de mutaciones exacto.
- UFC-STAB-102 local completo: hashes SHA-256 canónicos excluyen sólo el timestamp de observación, índices únicos parciales deduplican proyecciones/scoring nuevos y las filas legacy permanecen intactas con hash nulo. RED produjo 96 snapshots idénticos por tabla en 24 h; GREEN conserva uno y agrega otro únicamente ante un cambio material. Lecturas exponen `snapshotHash` y la suite completa pasa.
- UFC-STAB-103 local completo: `/health` conserva `runtime` y agrega `process`, `event_intel`, `ufc_stats`, `odds_api`, `billing` y `maintenance`. El payload revalida TTL, informa reason/último éxito y usa allowlists que excluyen tokens, paths, evidencias y cartelera. Billing y backup mantienen telemetría segura en memoria; Odds se lee desde su log SQLite sin auto-heal. Tests focalizados, suite completa, quality gate y `qa:parity:ufc` verdes.
- Checkpoint 2026-08-11: nueva auditoría de producción confirmó exactamente los síntomas que UFC-STAB-100/101 ya contienen (UFC 329 fantasma con fecha 2026-09-12 pese a haber ocurrido el 2026-07-11, cartelera mezclada con fragmentos "Preview"/"Gregor", `current_event` pegado en `Jiri Prochazka vs Carlos Ulberg` desde 2026-04-12). Se decidió no completar UFC-STAB-104 (ver fila en BACKLOG.md): el gate fail-closed ya construido resuelve ambos síntomas por TTL/quorum sin necesitar reetiquetado manual de filas productivas. Se encontró y eliminó un intento huérfano sin commitear de esa vía (`__tests__/ufcContainment.test.js` + registro en `runTests.js`), worktree vuelto a estado limpio.
- Plan aprobado (Fases A/B/C) en `/Users/flaviosirianniv2/.claude/plans/imperative-toasting-rainbow.md`: A = desplegar esta branch tal cual (UFC-STAB-105); B = discovery real de next_event vía Odds API (nuevo trabajo, fuera de esta branch); C = automatizar `data_scrapper` como timer diario en `ufc-oci` (nuevo trabajo, repo separado).
- **Deploy de contención ejecutado 2026-08-12 02:09:56 UTC** (commit `5d50462`, ff-only desde `1323308`): `ops/parity/ufc.required-keys.json` actualizado con las 3 env vars nuevas; agregadas a `/etc/bot-factory/ufc.env` del server (backup en `/etc/bot-factory/ufc.env.bak_20260812`); `npm test`/`quality:gate`/`qa:parity:ufc` en verde antes de mergear (nota: hubo que instalar `ripgrep` local, faltaba el binario y rompía `check-quality-pack.sh`, no relacionado al código). `git pull` + `systemctl restart bot-factory@ufc` en el server; `/health` respondió 200 a los 50s.
- Verificación productiva post-deploy: `/health` → `current_event`/`next_event` con `confidence:"invalid"`, `consumer_allowed:false`. Ciclo inicial de `preFightAnalysis` post-restart insertó **0** filas nuevas en snapshots (vs. 4+12 antes del deploy). Ledger idéntico al baseline 2026-07-20 (`bets=49, bet_mutations=52, credit_transactions=27, mp_processed_payments=1, ledger_summary=2`). Snapshot pre/post (`ufcOperationalSnapshot.js`) con `protected_tables` idénticos y `missing_protected_tables=[]` en ambos. Sin excepciones nuevas en journal.
- Hallazgo menor no bloqueante (abierto, sin dueño todavía): `/health.billing` reporta `status:"disabled"/reason:"billing_not_configured"` pese a que billing real funciona (confirmado con `GET /topup/config` → `enabled:true` en el mismo proceso, mismo `billingClient`). Bug cosmético aislado a `summarizeBilling()`/`getHealthStatus()` en `ufcHealthStatus.js`/`billingApiClient.js` — revisado en profundidad (env var confirmada en `/proc/<pid>/environ`, módulo aislado reproduce `enabled:true` con el mismo env), causa raíz no encontrada todavía. No afecta cobros/creditos reales. Anotado para investigar aparte, fuera del alcance de las Fases A/B/C.
- **Fase B ejecutada 2026-08-12** en branch `feature/ufc-odds-event-discovery`, commit `40bc357` (ff-only desde `5d50462`, mergeada a `main` y pusheada). TDD real: cada función nueva tuvo test RED antes de implementar (`resolveNextEventFromOddsRows` en `__tests__/oddsEventResolver.test.js`; casos de integración en `__tests__/eventIntel.test.js` y dos tests nuevos en `__tests__/bettingWizard.test.js` para los sitios de escritura de `current_event`). `npm test`/`quality:gate`/`qa:parity:ufc` verdes antes de mergear (tracker +4 símbolos, Q-0111..Q-0114).
- Deploy Fase B: snapshot pre/post con `protected_tables` idénticos y `missing_protected_tables=[]`; ledger sin drift (49/52/27/1/2, igual a Fase A); journal sin excepciones nuevas. `/health` confirma `next_event.event_name = "UFC: Islam Makhachev vs Ian Garry"`, `event_date_utc = "2026-08-15"` — el evento real (verificado contra fuentes externas antes de escribir código), reemplazando la UFC 329 fantasma. Queda en `degraded` (`compatible_source_count=1`) porque la corroboración de Google News no coincidió en este primer ciclo; el discovery reintenta cada 6h y debería subir a `verified` con más cobertura de noticias según se acerca la fecha (15-ago). Si en 1-2 días sigue en `degraded`, revisar `webCorroboratesOddsMainEvent()` en `eventIntel.js` (matching por apellido puede estar fallando contra el fraseo real de los titulares).
- **Fase C ejecutada 2026-08-12.** Repo `data_scrapper`: branch `fix/ufc-stats-pipeline` (commit `a906e5e`, mergeada), 10/10 tests pytest. Repo `ufc-orchestrator-bot`: branches `feature/ufc-data-scrapper-timer` (`a637dbe`) + `fix/ufc-stats-legacy-schema-compat` (`f3e36af`), ambas mergeadas a `main` y desplegadas.
- Infra en `ufc-oci`: Python 3.12 + venv + Playwright + Chromium en `/home/ubuntu/apps/data-scrapper` (repo clonado, sembrado con `state.json`/`fights.json`/`upcoming.json` reales del Mac). Confirmado que ufcstats.com no bloquea la IP del datacenter. Swapfile de 2GB agregado (persistente en `/etc/fstab`) antes de instalar Chromium — el server tenía <1GB RAM y 0 swap.
- **Corrida real completa 2026-08-12 14:02–14:38 UTC** (`systemctl start --wait ufc-data-scrapper.service`, ~34 min, dominado por los delays educados entre requests): 17 eventos nuevos desde abril, 208 fights nuevas, **24 excluidas correctamente como incompletas** (la cartelera de UFC 330, ya listada por ufcstats.com pero sin disputarse todavía — validación real del filtro de UFC-STAB-200 contra datos en vivo). Un timeout de red transitorio en una sola pelea, manejado sin abortar la corrida. Candidato validado (5287 fights vs. 5103 live) → swap atómico → restart → `/health` healthy en 77s. `ufc_stats.status` pasó de `degraded` a `healthy`, `age_hours=0.034`. Timer habilitado (`systemctl enable --now`), próxima corrida automática 2026-08-13 ~08:06 UTC.
- **Incidente durante el deploy de Fase C, detectado y resuelto en minutos**: al desplegar el fix de `ufcStatsTool.js` (usar `event_date_iso`), la DB live todavía tenía el schema viejo (sin esa columna en absoluto) — `getFreshnessMeta()` tiraba `no such column`, caía al catch y `/health` reportaba `fight_count:0` en vez de 5103. Detectado al chequear `/health` post-deploy (no fue silencioso). Fix: `PRAGMA table_info` para elegir la columna correcta según el schema real, test de regresión que reproduce el schema viejo exacto, commit `f3e36af`, redeploy inmediato. Confirmado en el siguiente `/health`: `fight_count` correcto de nuevo.
- Estado de recursos post-Fase C en `ufc-oci`: disco al 95% (2.6G libres) — no bloqueante hoy pero a vigilar, sobre todo si `fights.json` sigue creciendo. Swap se usó de verdad durante la corrida (645Mi de 2Gi) — confirma que hacía falta, no fue precaución de más.
- **Auditoría de completitud post-Fase C (2026-08-12, a pedido del usuario)**: conteo de eventos por año 2016-2026 consistente (~39-43/año, sin outliers); único mes sin eventos en toda la serie es 2020-04 (hiato real de UFC por COVID, no bug). Se encontraron y cerraron dos gaps reales el mismo día: (1) el fix de `meta`/`generated_at` en `data_scrapper` (commit `497dc20`, repo `data_scrapper`) se había hecho y testeado en la sesión anterior pero **nunca se commiteó ni pusheó** — el server seguía corriendo la versión sin tabla `meta`; (2) una pelea (Vicente Luque vs Kelvin Gastelum, evento `f3eb664db7fb1df3`) falló por timeout de red durante la corrida real y quedó fuera porque el scraper marca el evento entero como "scrapeado" en `state.json` aunque una pelea individual haya fallado (no hay retry granular por pelea) — re-scrapeada manualmente con `--event-id`. Ambos cerrados y verificados con una segunda corrida real del pipeline completo (`5288 fights`, `meta` poblada, ledger sin drift). Limitación conocida que queda abierta: si una pelea falla por red durante una corrida futura, no se reintenta sola — hay que detectarlo (revisando el log de esa corrida) y correr `--event-id` a mano. Mejora posible no implementada: que `_scrape_event` en `scraper.py` no marque el evento como completo si algún fight_stub no se pudo scrapear.
- Hallazgo menor no bloqueante (sigue abierto): `/health.billing` reporta `disabled` pese a que billing real funciona — ver detalle en el bullet de Fase A arriba. No investigado más en esta sesión.
- **Cierre de ventana de observación 2026-08-14** (commit `37887c7`): verificado contra `ufc-oci` en vivo, ~36h post-deploy (más de las 24h originales por continuidad de sesión, no por atraso). Ledger idéntico en todos los chequeos (`49/52/27/1/2`), `journalctl` de los últimos 2 días sin excepciones nuevas (sólo el patrón conocido y benigno de conflicto 409 de Telegram + auto-recovery), `/health` respondiendo 200 de forma estable. UFC-STAB-100/101/102/103/105/204 pasados a `DONE` en `BACKLOG.md`, fila resumen de Etapa 1 también actualizada.
- **Trabajo nuevo de la sesión, no relacionado al plan A/B/C**: implementación de `guided-menu-unification` (11 tareas vía subagentes con doble revisión cada una — spec compliance + calidad de código, con rondas de fix aplicadas y re-verificadas). Encontró y corrigió sobre la marcha varios gaps del plan original antes/durante la implementación (ver `docs/superpowers/plans/2026-08-12-guided-menu-unification.md` para el detalle técnico completo). Dos hallazgos reales quedaron en `WISHLIST.md` en vez de corregirse ahí mismo, por estar fuera de alcance: ítem 44 (bug preexistente en Ovidius, `GUIDED_INPUT_ACTIONS` sin acciones `med_*`, mitigado parcialmente el mismo día) e ítem 45 (falta manejo de rate-limit/`retry_after` de Telegram en el loop de envío multi-mensaje). Revisión final de branch completa confirmó los 6 criterios de aceptación del spec, suite y quality gate en verde. Mergeado a `main` (`bd796b5`) — **no desplegado a `ufc-oci` todavía**.
- **Confirmado en `/health` de producción, 2026-08-14 (post-cierre de ventana, con el server todavía en `f3e36af`)**: `current_event` sigue resolviendo a `"Matt Adams vs Anthony Wint"` (`event_id: b23487230c72d1a9c46a58cf02db58cc`, el mismo evento no-UFC de esta sesión de estabilización), con `confidence:"degraded"`, `consumer_allowed:false`. Esto es exactamente el bug que la Task 11 de `guided-menu-unification` arregla (filtro de roster UFC antes de declarar `current_event`) — el fix está en `main` pero no en el server. El `EventTruthGate` de esta sesión (UFC-STAB-100/101) está conteniendo el problema correctamente (`consumer_allowed:false` = no se expone a ningún usuario), así que no hay impacto real hoy, pero el dato contaminado sigue circulando internamente hasta que se despliegue el fix.

## Estado de datos protegido

- Ledger productivo: sin cambios en ninguno de los cuatro deploys de esta sesión (49 apuestas, 52 mutaciones, 27 movimientos locales, 2 resúmenes, 1 pago; conteos idénticos siempre).
- Billing productivo: servicio activo, no reiniciado en ningún deploy de esta sesión; DB no migrada ni reconciliada en esta etapa.
- `event_watch_state`, mirrors y snapshots productivos: las filas contaminadas (UFC 329, `current_event` viejo) se conservan sin purgar como evidencia histórica.
- `ufc_stats.db`: **fresca** por primera vez desde abril (`age_hours=0.034` al momento del deploy, va a subir hasta ~24h y resetear con cada corrida diaria del timer). Backup de la generación anterior en `/home/ubuntu/ufc-orchestrator-data/ufc_stats.db.bak` (se sobreescribe en cada corrida exitosa).

## Próximo paso exacto

1. **Desplegar `guided-menu-unification` a `ufc-oci`** (30 commits detrás, `f3e36af` → `main` actual). Seguir el checklist de "Deployment" propio del plan (`docs/superpowers/plans/2026-08-12-guided-menu-unification.md`): gates locales (`npm test && npm run quality:gate && npm run qa:parity:ufc`) ya verdes contra `main` al momento de este handoff; snapshot pre-deploy con `ufcOperationalSnapshot.js`; `git pull` + `systemctl restart bot-factory@ufc.service`; verificar `/health` 200 y ledger sin drift contra baseline (`49/52/27/1/2`); smoke test real por Telegram (botones de Registrar apuesta/Analizar quotes desde Evento y Análisis, confirmar que `current_event` deja de mostrar "Matt Adams vs Anthony Wint"). Esto es ahora la prioridad más alta: resuelve tanto la feature que pidió el usuario como un bug de datos ya confirmado activo en producción (contenido por el gate, pero sin arreglar de raíz).
2. Confirmar que las corridas automáticas del timer diario (`ufc-data-scrapper.timer`) siguen saliendo bien día a día (`journalctl -u ufc-data-scrapper.service`, `/health.ufc_stats.age_hours` debería resetear cerca de 0 después de cada corrida). Al momento de este handoff: `age_hours=7.6`, dentro de lo esperado.
3. UFC-STAB-204 sigue en `confidence:"degraded"` (`consumer_allowed:false`) después de 2+ días de reintentos cada 6h — no subió a `verified`. Si sigue así, revisar el matcher de corroboración (`webCorroboratesOddsMainEvent()` en `eventIntel.js`, ver bullet de Fase B arriba). Con `ufc_stats.db` ahora fresca, considerar sumar `upcoming_fights` como tercer corroborador (mencionado como pendiente en UFC-STAB-204). Este ítem vive formalmente bajo UFC-STAB-205 (`IN_PROGRESS`).
4. Vigilar espacio en disco de `ufc-oci` (95% usado al cierre de Fase C) si sigue creciendo.
5. En algún momento, investigar el hallazgo menor de `billing` en `/health` — no urgente, no bloquea nada.
6. Etapas 3-6 del backlog (billing global, runtime/polling duplicado, paths de datos) quedan fuera de esta sesión — proponer como fase separada, a confirmar con el usuario antes de arrancar.
7. WISHLIST ítems 44 (Ovidius, `GUIDED_INPUT_ACTIONS`) y 45 (rate-limit de Telegram en envío multi-mensaje) — reales, documentados, deliberadamente diferidos; no bloquean nada del punto 1.

## Rollback actual

Deploy Fase C 2026-08-12: revertir código con `git revert f3e36af a637dbe` (o `git checkout 40bc357 -- .` en el working tree del server) + `systemctl restart bot-factory@ufc.service`. Para revertir sólo el último swap de datos: `mv /home/ubuntu/ufc-orchestrator-data/ufc_stats.db.bak /home/ubuntu/ufc-orchestrator-data/ufc_stats.db` + restart (el script ya hace esto automáticamente si el health check post-swap falla). Deshabilitar el timer si hace falta: `sudo systemctl disable --now ufc-data-scrapper.timer`. Snapshots pre/post Fase C: `/home/ubuntu/pre-deploy-snapshot-faseC-20260812.json` (post no se capturó aparte, el post-fix ya quedó verificado vía `/health` + ledger). Deploys previos (Fase A/B): ver commits `5d50462`/`40bc357` y sus snapshots respectivos. No usar `reset --hard` ni tocar las DB directamente. Branches/worktree deben conservarse hasta cerrar la estabilización.
